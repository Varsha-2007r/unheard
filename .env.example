# Optional Google Gemini API Key for real LLM extraction (Fallback engine runs automatically if omitted)
VITE_GEMINI_API_KEY=

# Optional Google Maps API Key for satellite basemap (Custom Vector GIS Map runs automatically if omitted)
VITE_GOOGLE_MAPS_API_KEY=
